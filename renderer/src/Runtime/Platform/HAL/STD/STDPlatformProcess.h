#pragma once

#include "Platform/HAL/PlatformProcess.h"

class STDPlatformProcess : public PlatformProcess
{
public:
    //线程 ////////////////////////////////////////////////////////////////////////////////////////////////////////
	static RunnableThreadRef CreateRunnableThread();

    static void Sleep(float seconds);

    //信号量 可跨进程，多占有////////////////////////////////////////////////////////////////////////////////////////////////////////
	static SemaphoreRef CreateSemaphore(uint32_t maxCount);

    //事件 可跨进程，用于同步////////////////////////////////////////////////////////////////////////////////////////////////////////
    static SyncEventRef CreateSyncEvent(bool manualReset = false);	//TODO池化

    //临界区 单进程多线程，单占有////////////////////////////////////////////////////////////////////////////////////////////////////////
    static MutexRef CreateMutex();
};
